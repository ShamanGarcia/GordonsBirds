// Must match `basePath` in next.config.ts. next/link and next/image apply
// this automatically; plain <img> tags and fetch() calls to public/ assets
// need it prepended manually, since Next doesn't rewrite raw string paths.
export const basePath = "/GordonsBirds";
